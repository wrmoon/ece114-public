bool is_ready = true;

// The literals true and false can be converted to
// type int by promotion

int answer = true;          // answer is assigned 1
int promise = false;        // promise is assigned 0

// Any numeric or pointer value can be converted
// implicitly to a bool value
bool start = -100;          // start assigned true
bool stop = 0;              // stop assgined false


int main()
{
    // infinite/indefinite loop in c
    while(1)
    {
        // stuff 
    }

    // infinite/indefinite loop in c++
    while(true)
    {
        // stuff 
    }
}

